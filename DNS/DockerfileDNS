FROM alpine:latest

COPY DNS/dnsmasq.conf /etc/dnsmasq.conf

RUN apk add --no-cache dnsmasq

RUN apk add --no-cache dnsmasq bind-tools

RUN mkdir -p /var/log && apk add --no-cache dnsmasq bind-tools

EXPOSE 53

ENTRYPOINT [ "dnsmasq" ]
CMD [ "-k" ]

